<?php
// controller/AuthController.php

// Placeholder for future Authentication logic (Login/Signup POST Handling)
// Currently, authentication is mocked in JS with UI modals.
// Ensure future server-side auth connects model/User.php here before redirecting.

// Example:
// if ($_SERVER['REQUEST_METHOD'] === 'POST') {
//    // Process auth ...
// }

// By default, redirect to home if accessed directly
header('Location: index.php');
exit;
